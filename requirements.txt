# sentiment.py uses a HuggingFace transformer on CPU.
# torch is installed from the CPU wheel index in the workflow
# (--extra-index-url https://download.pytorch.org/whl/cpu) to avoid
# pulling the multi-GB CUDA build in CI.
transformers==4.53.2
torch==2.7.1
