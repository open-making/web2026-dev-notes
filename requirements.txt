# sentiment.py only needs TextBlob (pulls in nltk, regex, etc. as deps).
# numpy/scipy/scikit-learn were unused and broke CI on new Python versions
# by forcing a from-source scipy build (needs OpenBLAS). Removed.
textblob==0.19.0
